__pycache__/
*.py[cod]
.env
*.db
.venv/
venv/
node_modules/
dist/
.DS_Store
alembic/versions/*.pyc
agents/.agent_keys.json
camel/

# Private bull/bear world mapping for the hourly replay experiment — must
# never be committed, or it defeats the blind for the human analyst too.
experiments/.private_world_mapping.json
